# Esharq donors

Reserved for Esharq donor badge images. Each donor may pick their own image; drop it here and link it
from `../../badges.json` (discord id -> tooltip + image url). Donors then see the donation card.
